Please follow the steps to run this automation framework.

clone this directory
cd MOO
gem install rvm
gem install bundler
bundle install
from within the directory run the the following command in terminal 'cucumber features/product_search.feature'
